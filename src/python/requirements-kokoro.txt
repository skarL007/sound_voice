# Kokoro — qualidade, eficiente
# kokoro (PyTorch) só suporta Python <3.13. Em Python 3.13+ usamos kokoro-onnx
# (ONNX Runtime, sem torch), que suporta 3.10–3.13.
kokoro>=0.8.0; python_version < "3.13"
kokoro-onnx>=0.5.0
onnxruntime>=1.16.0
